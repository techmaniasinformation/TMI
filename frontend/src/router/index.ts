// 라우터 설정
import { createBrowserRouter } from 'react-router-dom'
import { ROUTES } from './routes'

// TODO: 실제 페이지 컴포넌트들을 import하여 라우터 설정
export const router = createBrowserRouter([
  // 라우트 설정 추가 예정
])

export { ROUTES }
export type { RouteKeys } from './routes'